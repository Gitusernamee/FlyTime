public class ActivityService
{
    private readonly IctivityRepository _ctivityRepository;

    public ctivityService(IctivityRepository ctivityRepository)
    {
        _ctivityRepository = ctivityRepository;
    }

}